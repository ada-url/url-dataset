export default {
  // Used to limit the output file line count
  target_output_size: 100_000,
  // Used to limit maximum links per domain
  maximum_links_per_root_domain: 50,
  // Maximum concurrent requests
  concurrency: 50,
  // Count of root domains extracted from top sites
  root_domain_count: 10,
}
